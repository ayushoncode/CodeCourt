# CodeCourt training dependencies
# Install this on Linux/Colab/HF GPU machines for GRPO and model fine-tuning.

-r requirements.txt

# Model / trainer stack
torch>=2.0.0
datasets>=2.20.0
accelerate>=0.34.0
transformers>=4.35.0
tokenizers>=0.15.0
trl>=0.15.2
peft>=0.4.0
wandb>=0.15.0

# Best used on Linux GPU machines; may fail on macOS due to xformers/build issues.
unsloth>=2024.1
