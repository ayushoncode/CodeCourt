# CodeCourt runtime dependencies
# Good for local env setup, Docker Space deploy, baselines, and evaluation.
# Heavy GPU training packages live in requirements-training.txt.

# Runtime / app
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pyyaml>=6.0
python-dotenv>=1.0.0
huggingface_hub>=0.24.0
docker>=7.1.0

# Evaluation / utilities
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0
tqdm>=4.65.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
